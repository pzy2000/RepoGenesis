[pytest]

python_files = test_*.py
python_classes = Test*
python_functions = test_*

testpaths = .

addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings

markers =
    integration: integration test
    slow: slow test

log_cli = false
log_cli_level = INFO

